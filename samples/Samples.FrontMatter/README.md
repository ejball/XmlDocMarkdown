# Samples.FrontMatter

Overrides `MarkdownPageRenderer.WriteFrontMatter` to emit page-level front matter.

The output demonstrates adding YAML front matter while keeping the normal Markdown body. [Widget.md](output/SampleAssembly/Widget.md) starts with the generated `title` front matter.
