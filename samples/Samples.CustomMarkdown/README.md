# Samples.CustomMarkdown

Overrides one Markdown section while reusing the standard page renderer.

The output demonstrates that a custom `MarkdownRenderer` can add content without replacing the whole renderer. [Widget.md](output/SampleAssembly/Widget.md) includes the custom generated note after the standard summary.
