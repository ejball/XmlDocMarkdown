# Samples.CustomCliOptions

Adds a host-specific CLI flag and usage line while delegating the rest of argument parsing to `XmlDocGenApp`.

The checked-in output uses that extra flag to generate public API only. [Widget.md](output/SampleAssembly/Widget.md) shows the public type page without the internal reset methods from the sample assembly.
