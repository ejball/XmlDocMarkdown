# Samples.CustomUrlMapper

Uses a custom `XmlDocUrlMapper` to produce an application-specific permalink shape.

The files are written to the default page layout, but their cross-links use `/api/...` HTML permalinks. [Widget.md](output/SampleAssembly/Widget.md) shows those custom URLs in its See Also section.
