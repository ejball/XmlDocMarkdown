# Samples.HtmlRenderer

Implements a minimal `XmlDocPageRenderer` that writes HTML instead of Markdown.

The output demonstrates replacing Markdown rendering entirely. [Widget.html](output/SampleAssembly/Widget.html) is an HTML page emitted by the custom renderer.
