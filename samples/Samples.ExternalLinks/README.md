# Samples.ExternalLinks

Combines external-link resolvers so references outside the generated tree can point to package or platform documentation.

The sample assembly references `string`, which is outside the generated documentation tree. [Format.md](output/SampleAssembly/Widget/Format.md) shows that external reference resolved through the configured link resolvers.
