# Samples.PagePerType

Generates Markdown with all members folded onto their containing type page.

The output demonstrates type-level grouping. [Widget.md](output/SampleAssembly/Widget.md) contains the constructor, property, and methods on one page instead of writing separate member pages.
